The Two-Job Ledger — Mick's PG vs BA utilisation report.

`index.html` is the whole site. Rebuilt every evening at 9pm by the
`two-job-ledger` scheduled task, which pushes here; GitHub Pages redeploys.

Unlisted: `noindex` on the page, no inbound links anywhere. Share the URL
deliberately.
